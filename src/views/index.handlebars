<div class="container-fluid text-center">
    <div class="row content">
        <div class="col-lg-2 sidenav">
            <div class="ads">
                <ads></ads>
            </div>
            <div class="ads">
                <ads></ads>
            </div>
        </div>

        <div class="col-lg-9 text-justify">
            <div class="saloes row" id="saloes">
                {{#each saloes}}
                <div class="col-sm-6 col-md-4 col-lg-4 col-xl-4 mb-2 border border-dark" id="{{id}}">
                    <img src="./img/{{nome}}.jpg">
                    nome: {{nome}} <br>
                    cidade: {{cidade}} <br>
                    cnpj: {{cnpj}} <hr>
                </div>
                <br>
                {{/each}}
                  {{#each saloes}}
                <div class="col-sm-6 col-md-4 col-lg-4 col-xl-4 mb-2 border border-dark" id="{{id}}">
                    <img src="./img/{{nome}}.jpg">
                    nome: {{nome}} <br>
                    cidade: {{cidade}} <br>
                    cnpj: {{cnpj}} <hr>
                </div>
                <br>
                {{/each}}
            </div>
        </div>
        <div class="col-lg-1 sidenav">
            <div class="ads">
                <ads></ads>
            </div>
            <div class="ads">
                <ads></ads>
            </div>
        </div>
    </div>
</div>