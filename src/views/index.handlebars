<div class="container-fluid text-center">
    <div class="row content">
        <div class="col-lg-2 sidenav">
            <div class="ads">
                <ads></ads>
            </div>
            <div class="ads">
                <ads></ads>
            </div>
        </div>

        <div class="col-lg-9 text-justify">

            <div class="saloes row" id="saloes">
                {{#each saloes}}
                <div class="card col-4">
                    <div class="card-img-top"><img src="../assets/img/{{nome}}.jpg" style="height: 200px; width: auto;"></div>
                    <div class="card-body">
                        <h5 class="card-title">{{nome}}</h5>


                        <p class="card-text">{{cidade}}</p>
                        <p class="card-text">{{descricao}}</p>
                        <div>
                            <a class="btn btn-dark btn-block" href="/saloes/{{id}}" id="{{id}}">Entrar</a>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>

        <div class="col-lg-1 sidenav">
            <div class="ads">
                <ads></ads>
            </div>
            <div class="ads">
                <ads></ads>
            </div>
        </div>
    </div>
</div>